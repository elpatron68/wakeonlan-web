services:
    wakeonlan-web:
        network_mode: "host"
        environment:
            DATABASE_HOST: "127.0.0.1"
        ports: null

    mariadb:
        network_mode: "host"
        ports: null
        environment:
            MARIADB_ROOT_HOST: "%"

